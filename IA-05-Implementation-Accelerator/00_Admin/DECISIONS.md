# Decisions — IA-05

Every decision that changes scope, data, tooling or submission behaviour goes here, with its rationale and the evidence behind it. A decision that only lives in a chat thread does not exist.

Rows 1–5 are decisions the team has **already taken**. They are team decisions, not sponsor requirements — anything needing sponsor confirmation is flagged in its own column.

| Date/time PT | Decision | Rationale | Evidence or source | Needs sponsor confirmation? | Owner |
| --- | --- | --- | --- | --- | --- |
| `<TODO>` | **Single self-contained HTML file, vanilla JS, no build step.** *(Team decision.)* | Review is asynchronous, 18–25 September 2026. Reviewers open the package days later with no access to our tools and no live explanation, so the artifact must open with no install and behave identically for every reviewer. | Event rules: asynchronous judging; deliverable must run with no setup | No | `<TODO: name>` |
| `<TODO>` | **AI used at authoring time only; the shipped artifact performs no inference.** *(Team decision.)* | No key can ship in a shared file — the rules forbid credentials in any shared file — and removing run-time inference makes the demo reproducible for every reviewer. | Event rules: never place credentials, API keys, secrets or access tokens in a prompt, connector, repository or shared file | No | `<TODO: name>` |
| `<TODO>` | **No RAG, vector store or embeddings index.** *(Team decision.)* | The entire approved corpus is roughly 24,500 tokens and fits in a single model context. Retrieval would add infrastructure we cannot ship and a hallucination risk against the accuracy criterion. | Token count of the sponsor-provided source set: ~24,500 tokens | No | `<TODO: name>` |
| `<TODO>` | **Product dependency logic implemented as a deterministic rule engine.** *(Team decision.)* | The dependency rules are documented in the sponsor's product dependency document, so a rule engine's output is provable and citeable — a model's would not be. | `<TODO: cite the product dependency document and section>` | No | `<TODO: name>` |
| `<TODO>` | **Primary scope is Engagement Coordinator / Add-On.** Other role and implementation-type combinations are tagged in the data but left unfilled and shown to the user as gaps. *(Team decision.)* | 24 hours is not enough to cover every role × implementation type against real sources. Tagging and showing the gaps is honest; filling them with invented content is not. | Sponsor-provided source set covers the EC Add-On path; other paths lack source coverage | `<TODO: confirm with sponsor that EC/Add-On is the right first slice>` | `<TODO: name>` |
| `<TODO>` | **Stage-to-SOP mapping is an interpretive choice.** The seven Add-On journey stages are used as the data's stage ids; each is mapped to an EC SOP v3 section (§9.1 → `assignment`, Orders Review/Salesforce decks → `research`, §9.2 first half → `introduction`, §9.4 → `data`, §9.2 second half → `scheduling`, §9.5 → `escalation`, §9.7 → `eoe`). *(Team decision.)* | The stage **names** are verbatim — the Add-on Projects and Processes Review deck, slide 4 lists exactly these seven as the Add-On Journey. What is interpretive is the **mapping of each stage to an SOP section**: the SOP numbers its procedures 9.1–9.7 but does not label them with journey stage names. A wrong mapping puts the wrong checklist in front of a learner at that point in the journey. | Add-on Projects and Processes Review deck, slide 4 (stage names, verbatim); EC SOP v3, §9.1–§9.7 (section content) | **Yes — needs SME confirmation from becky.tiner@, sarah.jackson@, kat.ragudos@realpage.com** | `<TODO: name>` |
| `<TODO>` | **The artifact inlines its JSON rather than fetching it.** `app.html` carries `modules.json`, `products.json` and `sources.json` in `<script type="application/json">` blocks. *(Team decision.)* | The reviewer README promises the artifact opens by double-clicking. From a `file://` URL the browser's origin rules block loading a sibling JSON file, so a fetching version would show an empty page for exactly the reviewer we are building for. The cost is two copies of the data, so `check.js` asserts the inline copies deep-equal the canonical files in `02_Working/data/` — they cannot drift apart silently. | Event rules: asynchronous judging, deliverable must run with no setup | No | `<TODO: name>` |
| `<TODO>` | **Every module ships unapproved; the approval gate starts closed.** All nine modules carry `review: "drafted — awaiting SME review"`, and the Learner view withholds them. *(Team decision.)* | No RealPage SME has reviewed the drafted content, so marking anything approved would be a fabricated review status — the one thing the brief's compliance criterion is most directly about. The gate is a real client-side filter, not a badge. The Learner view's empty state explains this and offers a clearly-labelled draft preview so a hackathon reviewer can still read the content; the Owner view records approvals against a required reviewer name, in that browser only. `check.js` asserts no module ships pre-approved. | Brief, §4 Evaluation Criteria: "preserve human Professional Services judgment"; Reference Workflow Diagram: "human SME review" | No | `<TODO: name>` |
| `<TODO>` | **`extract-text` was not available, so the team's own extractor was written.** `02_Working/tools/extract-text.js` converts the 14 Office files to plain text. *(Team decision.)* | The named `extract-text` utility is not installed on the build machine. Office files are ZIP archives of XML, so extraction needs no model, no key and no network — Node's built-in `zlib` plus the ZIP central directory is enough. This keeps ingestion deterministic and adds no dependency. The one PDF (the brief) was read directly and is recorded as `mode: "pdf-text"`. | `02_Working/tools/README.md`; 14 of 14 files extracted with 0 failures | No | `<TODO: name>` |
| `<TODO>` | **The Leader view reports structural coverage only, not learner progress.** *(Team decision.)* | No learner has used the artifact, so there is no completion, score, time-on-task or confidence data. Inventing any of it would be fabricated evidence. The view counts what is actually there — stage coverage, the self-paced/live split, scenario coverage, gate status, open `<TODO:` count — and says plainly at the top that it is not a live progress tracker. | Event rules: no fabricated metrics; brief hero feature "Onboarding progress and confidence view" | No | `<TODO: name>` |
| `<TODO>` | **The four original SME questions were re-counted and corrected.** The tools checklist holds **20** items with **16** unjustified (not 18/14), and the Onboarding Links sheet holds **19** titles (not 18). Four further gaps (GAP-05 to GAP-08) were found during extraction. *(Team decision.)* | The original figures came from a spot-check. Quoting a wrong count to an SME wastes the one round-trip we get, and the accuracy criterion applies to our own gap reporting as much as to module content. Counted from the extracted workbook. | `01_Sponsor_Inputs/extracted/Onboarding_Training_Agenda_Calendar_for_EC_Role_V2.txt`, Checklist and Onboarding Links sheets | No | `<TODO: name>` |
| `<TODO>` | **`rum` keeps `requires: ["onesite"]` as a flagged working assumption.** *(Team decision.)* | RUM/RUSM is absent from the Product Dependency Reference table, and neither dependency document states a OneSite prerequisite. Dropping the dependency would make the planner sequence RUSM first, which contradicts the Project Planning Process deck ("usually the last to be implemented"). Keeping it, flagged in `requiresSrc` and surfaced in the artifact's planner, is the least-wrong option — but it is an assumption, not a source fact. | Product Dependency Workflow docx, Product Dependency Reference table (21 products, no RUSM); Project Planning Process deck, slide 3 | **Yes — GAP-06 in `SME_QUESTIONS.md`** | `<TODO: name>` |
| `<TODO>` | **A real PMC name found in the source was replaced with `PMC`.** The SOP's §9.3 meeting-invite example carries a real property-management-company name in the subject line; modules render it as "PMC – RealPage \| Payments Kick Off". *(Team decision.)* | The rules forbid client names in the package. `check.js` asserts the name does not appear anywhere in the shipped data, so a future edit cannot reintroduce it unnoticed. | EC SOP v3, §9.3; SOP Review1 deck, slide 16 | No | `<TODO: name>` |
| `<TODO>` | `<TODO: decision>` | `<TODO: rationale>` | `<TODO: evidence or source>` | `<TODO: yes/no>` | `<TODO: name>` |

---

**Fair-play note.** Any SME answer that changes scope, data, tooling or submission behaviour must be recorded here **and shared with the other teams working the same use case**. See `SME_QUESTIONS.md`.
