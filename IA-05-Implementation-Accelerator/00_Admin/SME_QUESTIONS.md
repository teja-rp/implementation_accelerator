# SME questions — IA-05

Open questions for the subject-matter contacts. Ask here, record the answer here, then act.

**Subject-matter contacts (use these, add none):**
becky.tiner@realpage.com · sarah.jackson@realpage.com · kat.ragudos@realpage.com · megan.sellers@realpage.com · caitlin.bowen@realpage.com

**Event logistics:** austin.braham@realpage.com

Questions 1–4 are **genuine gaps observed in the sponsor-provided source set**, not hypotheticals. Until they are answered, the affected modules stay marked as gaps rather than being filled with a guess.

| Question | Why it matters | Who can answer | Asked on | Answer | Recorded in |
| --- | --- | --- | --- | --- | --- |
| The EC SOP's "Project Assignment and Capacity Tracking" section is marked **"Coming soon"**. Is there a current interim process? | The `assignment` stage is the first stage of the Add-On journey. Without this we cannot write the first module a new EC ever sees, and we will not invent a process. | `<TODO: which contact>` | `<TODO: date PT>` | `<TODO: answer, verbatim>` | `<TODO: module id / file>` |
| The EC SOP's Document Control effective date is blank — it reads **"[Insert Date]"**. What version and effective date should modules cite? | Every module carries a source citation. A citation to an undated document is not verifiable, and the accuracy criterion depends on citations a reviewer can check. | `<TODO: which contact>` | `<TODO: date PT>` | `<TODO: answer, verbatim>` | `<TODO: sources.json / module source fields>` |
| The onboarding tools checklist lists **18 tools and compliance courses**, but only **four** carry a ticket justification. Are the others documented anywhere? | The tools checklist drives the Day-1 access modules. Fourteen entries with no documented justification means we cannot tell a learner why or how to request them. | `<TODO: which contact>` | `<TODO: date PT>` | `<TODO: answer, verbatim>` | `<TODO: module id / file>` |
| The **"Onboarding Links"** sheet lists **18 session titles with no destinations**. Where do these live? | These are the facilitated (live) half of the self-paced / facilitated split shown in the Leader view. Without destinations we can name the sessions but not point to them. | `<TODO: which contact>` | `<TODO: date PT>` | `<TODO: answer, verbatim>` | `<TODO: module id / file>` |
| `<TODO: question>` | `<TODO: why it matters>` | `<TODO: which contact>` | `<TODO: date PT>` | `<TODO: answer>` | `<TODO: where recorded>` |

---

**Fair-play rule.** Any answer that changes **scope, data, tooling or submission behaviour** must be recorded in `DECISIONS.md` **and shared with the other teams working this same use case**. An SME answer is a shared input, not a competitive advantage.

Answers that only clarify content for our own modules still get recorded in the table above so the citation trail stays intact.
