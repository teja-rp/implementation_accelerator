# Sources and permissions — IA-05

## Sources

Three separate permission questions per source. Access does not imply permission to use in a given tool, and using a document during authoring does not imply permission to ship it.

| Source | Sponsor-provided or team-created | Can access? | Can use in this tool? | Can include in package? | Notes |
| --- | --- | --- | --- | --- | --- |
| `<TODO: document name, exactly as provided>` | `<TODO: sponsor-provided / team-created>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: notes — e.g. cited by name and section only, content not reproduced>` |
| `<TODO: document name>` | `<TODO: sponsor-provided / team-created>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: notes>` |
| `<TODO: document name>` | `<TODO: sponsor-provided / team-created>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: yes/no>` | `<TODO: notes>` |

Where "Can include in package?" is no, the source is **cited by name and section** and its content is not reproduced.

---

## Reused versus created during the event

### Reused — existed before the event

- `<TODO: item, e.g. a sponsor-provided document>`
- `<TODO: item>`

### Created during the event

- `<TODO: item, e.g. the HTML artifact>`
- `<TODO: item, e.g. the module and product data files>`
- `<TODO: item>`

---

## Client names

Source examples in the sponsor-provided set may contain **real client names**. Every customer, client, property and property-management-company name appearing anywhere in this package has been replaced with the literal placeholder **`PMC`**.

This was applied to module content, practice scenarios, screenshots and the walkthrough recording — not only to the data files.

`<TODO: name of the person who verified this, and date/time PT>`

---

## Credentials

**No credentials, keys or tokens appear anywhere in this package.**

The artifact performs no inference at run time and makes no network calls, so it needs no credential to function. Nothing in the package — file, comment, screenshot, recording or accompanying message — contains a credential, API key, secret, access token or connection string.

No custom MCP server was created, installed or configured for this event.
